# Healthcare Appointment Scheduler (Minimal Example)

## Prerequisites
- Node.js 18+
- MongoDB running locally (default port)
- Optional: Twilio credentials for real SMS

## 1) Backend
cd server
npm install

Create `.env`: