# .github/workflows/reusable-workflow.yml
on:
  workflow_call:
    inputs:
      my_variable:
        required: true
        type: string

jobs:
  use-variable:
    runs-on: ubuntu-latest
    steps:
    - name: Use the passed variable
